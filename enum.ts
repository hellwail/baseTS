enum Membership {
    Simple,
    Standart,
    Premium
}

const membership = Membership.Standart
const membershipReverse = Membership[2]

enum SocialMedia {
    VK = 'VK',
    FB = 'FACEBOOK',
    INST = 'INST'
}

const social = SocialMedia.FB